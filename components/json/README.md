# JSON

This is a plain copy of original ESPHome json component but with loading from github instead of platformio.

## Usage

```yaml
external_components:
  - source: github://dentra/esphome-components
    components: [json]
```
